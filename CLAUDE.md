# CLAUDE.md

甲子園(春の選抜 / 夏の選手権)の本大会データを Wikipedia から収集し、
PostgreSQL(Supabase)へ取り込むスクレイパ。対象は **1978年(1県1代表化)以降**。

背景・設計判断の詳細は `README.md`、DBスキーマの意図は `koshien_db_design.md` にある。
このファイルには、コードを読むだけでは分からない前提と落とし穴を書く。

## コマンド

Python環境は **uv** で管理する。`pip install` や `requirements.txt` は使わない。

```bash
uv sync                        # 依存を uv.lock どおりに再現
uv run pytest                  # 回帰テスト(フィクスチャは conftest.py が自動生成)
uv run pytest -k list_format   # 単一テスト
uv run ruff check .            # lint
uv run ruff check --fix .      # 自動修正
uv add <pkg>                   # 依存追加(--dev で開発用)
```

CLI(`[project.scripts]` で定義。`python -m koshien.cli` でも可):

```bash
export KOSHIEN_UA='koshien-db/0.1 (name; you@example.com)'   # 未設定だと起動時に失敗する

uv run koshien fetch    --from 1978 --to 2025          # 記事の存在確認 + キャッシュ
uv run koshien parse    --from 1978 --to 2025          # パース + 検証 → data/parsed/*.json
uv run koshien validate --from 1978 --to 2025 -v       # 検証のみ再実行
uv run koshien debug    --from 1978 --to 1978 --seasons spring   # 実記事の構造をダンプ
uv run koshien load     --from 1978 --to 2025 --dry-run
```

**デバッグの起点は必ず `debug`。** 記事の見出し・各テーブルの先頭3行・箇条書きを
ダンプするので、「どの表を拾えていないか」が一目で分かる。
パーサを当て推量で直す前にこれを実行すること。

## アーキテクチャ

パイプラインは4段。各段は独立して再実行でき、途中結果はディスクに残る。

```
wiki.py    MediaWiki API から取得 → data/cache/*.json(再実行時はネットワークに出ない)
parse.py   HTML → TournamentData(entries[], games[])
validate.py トーナメント構造による整合性チェック
load.py    PostgreSQL へ冪等 UPSERT
```

`normalize.py` は全段が使う共通部品(文字列正規化、都道府県⇔地区⇔地方大会の対応表)。
`titles.py` は年・季節から回次と記事タイトル候補を生成する(春 = 年 − 1928、夏 = 年 − 1918)。

## 記事書式が3系統ある — ここが本体

同じ「大会記事」でも年によって試合結果の書き方が違う。`parse.py` はこれを吸収する層で、
コード量の大半を占める。**新しい年に対応するとき、まずどの系統かを見極めること。**

| 系統 | 年代の目安 | 関数 |
|------|-----------|------|
| A. 一覧表(勝利/スコア/敗戦の列) | 概ね2000年代以降 | `parse_games_table()` |
| B. 箇条書き(`天理 6 - 0 松商学園`) | 概ね1990年代以前 | `parse_games_list()` |
| C. イニングスコア表(スコアボード) | 全年代。主に決勝 | `parse_linescores()` |

`parse_games()` が A を試し、10試合未満なら B に切り替え、**最後に必ず C で補完**する。
C は A/B から漏れた試合(実記事では決勝がここにしかないことが多い)を拾うための段で、
スキップしてはいけない。

### 実データで踏んだ地雷(再発させないこと)

- **ヘッダ行は先頭行とは限らない。** 出場校表の1行目が `colspan` つきの
  「東日本 / 西日本」見出しで、列名は2行目にある。先頭行決め打ちにすると全年で
  出場校0件になる。`_find_header_row()` が先頭4行を探索する。試合結果表も同様。
- **決勝が一覧表・箇条書きに無い年がある。** イニングスコアだけで載っている。
  これを拾わないと `no_final` と「試合数が1件足りない」が同時に出る。
- **先攻/後攻は原則不明。** 取得元が持つのは勝利校・敗戦校・スコアであって打順ではない。
  判明するのは (1) スコアの `x`(サヨナラ = 勝者が後攻)、(2) イニングスコア表の上段(= 先攻)
  の2ケースのみ。DBスキーマもこれに合わせてある(後述)。
- **ラウンド見出しが無い記事がある。** その場合 `assign_rounds_by_bracket()` が
  トーナメント構造から逆算する(末尾から 決勝1・準決勝2・準々決勝4…、残りが1回戦)。
  49代表なら 17+16+8+4+2+1 = 48試合になる。
  **番号付きラウンド(r1/r2/r3)の名前は末尾からの深さで決めず、先頭から昇順に振る。**
  ラウンド数が年で違うため。夏49代表は6ラウンドで8試合のラウンドが3回戦だが、
  春32校は 1回戦16・2回戦8・準々決勝4… の5ラウンドで、8試合のラウンドは**2回戦**。
  `validate.py` のラウンド数チェックも同じ理由でコード名ではなく位置で見る。
- **不戦勝(出場辞退)の試合はスコアを持たない。** ブラケットでは勝者セルが
  「〇〇(不戦勝)」・スコア列が空で、辞退校がその隣に並ぶ。スコア必須の `parse_bracket()`
  はこれを落とし、`game_count`(試合数不足)と `champion`(辞退校が敗戦を持たず無敗のまま)が
  同時に出る。`parse_bracket()` が不戦勝ノードを検出し
  `GameRow(is_forfeit=True, スコア=None)` として記録する(勝者=進出校・敗者=辞退校)。
  **サヨナラ(`is_walkoff`)とは別概念。**
  - **勝者と辞退校のどちらが上に並ぶかは年による**(並びは組み合わせ順であって
    勝敗順ではない)。2021夏(第103回)は勝者が先、2022春(第94回、広島商のコロナ辞退)は
    **辞退校が先**。片方の順序だけを見るとスコア無しの行を捨ててしまう。
  - **不戦勝ブロックには日付行が無いことがある**(2022春)。直前ブロックの日付を
    持ち越すと、行われていない試合に日付が付く。日付行を見た時点の位置を覚えておき、
    そのブロックのものでなければ使わない。
  - フィクスチャは `build_forfeit_summer()`(勝者が先)と
    `build_forfeit_spring()`(辞退校が先・日付行なし・春32校のラウンド割り当ても兼ねる)。

## 検証はトーナメントの性質を使う

`validate.py` はパーサの安全網であり、**ここを緩めて通すのは筋が悪い**。
エラーが出たら記事側の書式差を疑い、パーサを直す。

主なチェック: 試合数 = 出場校数 − 1(+再試合)、2敗している学校がない、
無敗がちょうど1校(= 優勝校)、決勝1試合・準決勝2試合…(末尾から数える。最初の
ラウンドは byes を含むので数えない)、夏なのに地方大会が空 / 春なのに入っている、
都道府県が解決できない出場校がある。

`parse` はエラーがあっても JSON を書き出すが、**エラーが残る大会は `load` しない**運用。

## テストの流儀

サンドボックス/CI から `ja.wikipedia.org` に出られない前提で、
**実記事の構造を再現したフィクスチャ**(`tests/make_fixtures.py` が生成)でテストする。
`tests/fixtures/` は生成物なので直接編集しない。

新しい書式に対応するときの順序を守ること:

1. `uv run koshien debug` で実記事の構造を確認する
2. `make_fixtures.py` にその構造を再現したフィクスチャを**先に**追加する
3. パーサを直す
4. `uv run pytest` で既存の年が壊れていないことを確認する

フィクスチャを先に足すのは、ある年への対応が別の年を壊す事故を防ぐため。
実際に「ヘッダ行=先頭行」の思い込みで全年が壊れた経緯がある。

## DBスキーマで注意する点

`supabase/migrations/` のマイグレーションを適用(`supabase db push`、または SQL Editor で
番号順に実行)してから `load` する。

- **春と夏で代表の階層が違う。** 夏 = 代表校 / 地方大会 / 都道府県 / 地区、
  春 = 代表校 / 都道府県 / 地区。地方大会は夏のみで、春の行では必ず NULL。
  21世紀枠は春のみのフラグ(地区とは独立。21世紀枠校もいずれかの地区に属する)。
- **記念大会の分割枠は年によって東西/北南が違う。** 2008・2018は北南(北埼玉/南神奈川…)
  だが、**1998(第80回)だけは東西**(東埼玉・西埼玉・東神奈川・西神奈川)。
  地方大会名は `normalize.QUALIFIER_TO_PREF` と `summer_qualifiers` のシードの
  **両方**に足すこと。片方だけだと `load` が該当校の entry を捨て、その校が絡む試合も
  芋づるで落ちる(1998夏で 4 entry + 13 試合 = 17件。優勝校の横浜が丸ごと欠けていた)。
  整合は `tests/test_master_data.py` が migrations の SQL を読んで検査する。
- **この制約は CHECK 制約で DB が保証している。** CHECK は他テーブルを参照できないため、
  `entries.season` を非正規化し、`tournaments (id, season)` への複合FKで
  親との一致を担保している。`entries` に列を足すときこの仕組みを壊さないこと。
- `games` は `entry1_id` / `entry2_id`(掲載順。確定試合では entry1 = 勝利校)。
  先攻は `first_bat_entry_id` に判明時のみ入る。一意キーは向きに依存しないよう
  生成列 `pair_lo` / `pair_hi` を使う。
- `games.status` は `final` / `draw` / **`forfeit`(不戦勝)** / `no_game` / `scheduled`。
  不戦勝は**進出校を `winner_entry_id` に設定**し(=勝者あり)、試合が行われないため
  `score1` / `score2` は NULL。スキーマ(`supabase/migrations/20260813000001_init_schema.sql`)は
  enum・NULL 許容とも対応済みで、`games_status_winner_ck` が **final と forfeit のみ勝者必須**、
  `games_forfeit_no_score_ck` が forfeit のスコア NULL を保証する。この2制約を壊さないこと。
- 集計は `v_game_teams`(1試合を2行に展開したビュー)を使うと CASE 式が消えて楽。
- 全 UPSERT は自然キー対象なので**何度実行しても結果は同じ**。この性質を壊さない。

## 学校の名寄せ

校名は「大阪桐蔭 / 大阪桐蔭高 / 大阪桐蔭高等学校」と揺れ、旧校名(明徳 → 明徳義塾)や
**同名校が別県に存在する**問題がある。`normalize_school()` で正規化したうえで、
**必ず都道府県で絞ってから**照合する。校名だけで名寄せしてはいけない。
未知の校名は `--auto-create-schools` 指定時のみ新規作成し、記事中の表記を
`school_aliases` に残す。

**記事側の誤記もここで吸収する。** 2025春はブラケットだけ「ニ松学舎大付」(カタカナのニ)と
書かれており、出場校表の「二松学舎大付」と照合できず `unknown_school` になっていた。
`normalize_school()` の `_CHAR_MAP` で名寄せキーだけを直す(記事の表記は残す)。

## 未解決の課題

- **春の記事の書式が未確認。** 手元で `games=0` になる報告があり、
  A/B/C のいずれとも異なる書式(トーナメント表テンプレート等)の可能性がある。
  まず `debug --seasons spring` の出力を見ること。
- **1985夏のラウンドが全て1回戦になっている**(48試合中47試合が r1、決勝のみ f)。
  記事の見出しから `cur_round` が引きずられているのが原因と思われる。検証は
  最初のラウンドを数えないため素通りする(= エラーにならないまま間違っている)。
- 21世紀枠の判定が春の記事の表構造に依存しており、取りこぼす年がありうる。
- `replay_of_game_id`(再試合の親試合)は未設定。検出(`replay_seq`)まではしている。
- 2020年は春夏とも中止のため `titles.CANCELLED` で除外している。

## スクレイピングの作法(緩めない)

- `KOSHIEN_UA` は連絡先入りを必須とし、未設定なら `WikiClient.__init__` で例外にする。
  Wikimedia の User-Agent ポリシーへの対応であり、この検証を外さないこと。
- リクエスト間隔は既定1秒、`maxlag=5` を付与。取得結果は必ずキャッシュする。
- 出典は `?oldid=` 付き URL で保存する(記事は CC BY-SA)。
